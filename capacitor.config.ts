import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trainingform.app',
  appName: 'TrainigForm',
  webDir: 'out',
  server: {
    androidScheme: 'http',
  },
};

export default config;
